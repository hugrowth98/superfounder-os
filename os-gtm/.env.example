# OS-GTM - Variables d'environnement (gabarit)
# Copiez ce fichier en .env (install.sh le fait pour vous). Le .env est ignore par git.
# Rempli automatiquement par le skill "connecter-outils". Rien a copier ici a la main.
# Le .env rempli ne doit JAMAIS etre partage ni inclus dans un ZIP envoye a un client.

# ── LINKEDIN / SALES NAVIGATOR ───────────────────────────────────────────
UNIPILE_API_KEY=
UNIPILE_DSN=
UNIPILE_ACCOUNT_ID=

# ── DONNEES B2B (ENTREPRISES ET PERSONNES) ───────────────────────────────
CRUSTDATA_API_KEY=

# ── ENRICHISSEMENT CONTACT (FullEnrich) et CAMPAGNES (Lemlist) ────────────
# Connectes via serveur MCP officiel, pas de cle ici.
